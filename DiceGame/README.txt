You can find DiceGame.jar file that can be executed in the DiceGame_jar catalogue.

	~\DiceGame_jar\DiceGame.jar

To execute it, write:

	java -jar DiceGame.jar N K M

,where 
2 <= N <= 6
2 <= K <= 5
1 <= M <= 100